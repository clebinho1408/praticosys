{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck:libs && pnpm --filter @workspace/praticosys run typecheck && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/praticosys run build",
    "build:all": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "cf-build": "pnpm run typecheck:libs && pnpm --filter @workspace/praticosys run typecheck && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/praticosys run build"
  },
  "private": true,
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.43.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/bcryptjs": "^3.0.0",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
